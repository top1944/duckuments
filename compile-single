#!/bin/bash
set -e
set -x



mcdp-render \
    --stylesheet v_manual_screen \
    --maindir `dirname $1` \
    --config_dirs `dirname $1` \
    --symbols docs/symbols.tex\
    --forgiving \
    "$1"

# python -m mcdp_docs.add_edit_links 
